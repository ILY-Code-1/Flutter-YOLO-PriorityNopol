abstract class AppRoutes {
  static const String home      = '/home';
  static const String detection = '/detection';
  static const String result    = '/result';

  // /preview dihapus — preview sekarang in-place di dalam DetectionView
}
